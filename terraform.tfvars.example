# Renombra este archivo a terraform.tfvars y ajusta los valores
# El archivo terraform.tfvars no debe subirse a git (está en .gitignore)

aws_region   = "us-east-1"
environment  = "dev"
project_name = "lab-vpc"

# vpc_cidr = "10.0.0.0/16"
# availability_zones = ["us-east-1a", "us-east-1b"]
